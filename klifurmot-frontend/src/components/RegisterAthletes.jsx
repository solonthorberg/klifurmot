function ControlPanelComponent({ goBack }) {
    return (
        <>
            <button onClick={goBack}>← Til baka</button>
            <p>Mót</p>
        </>
    )
};

export default ControlPanelComponent;